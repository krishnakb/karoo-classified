<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Legacy Bluetooth permissions, needed on Karoo 2 (API 27). -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- BLE scanning requires location on API 27-30. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Modern Bluetooth permissions, used on Karoo 3. -->
    <!-- neverForLocation: we only want sensor data, never position. Without this
         flag Android 12+ also demands ACCESS_FINE_LOCATION or scans return empty. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Declared so the ANT probe can report whether it is grantable at all. -->
    <uses-permission android:name="com.dsi.ant.permission.ANT" />

    <!-- Android 11+ filters package visibility. Without this, getPackageInfo()
         throws NameNotFoundException for these even when they are installed,
         which reads as "absent" and wrongly rules out the ANT+ path. -->
    <queries>
        <package android:name="com.dsi.ant.service.socket" />
        <package android:name="com.dsi.ant.plugins.antplus" />
        <package android:name="com.dsi.ant.server" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".ProbeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
